# アーキテクチャ

<!--
「どう作るか」の方針。開発者と AI エージェントが常時読む文書。
頻繁に変わらない内容だけを置く。コードと同期させようとせず、年に数回見直す程度に保つ。
機能を足すたびに変わる情報は design.md へ。
-->

## 全体構成図

<!-- システムを構成する要素と、その配置・通信経路。コンポーネント図または配置図。 -->

```mermaid

```

## 技術選定と理由（ADR）

<!--
なぜその技術・構成を選んだかの記録。「変わらないが後から必ず参照される」情報。
追記のみのログとして扱う。一度書いた記録は編集せず、決定が変わったら新しい記録を足して2つをリンクする。
1件あたり: 決定 / 背景（何を解決したかったか）/ 却下した選択肢と却下理由 / 結果として受け入れたトレードオフ。
-->

## レイヤー構成と依存の方向

<!--
どんな層に分け、依存をどちら向きに流すか。逆流を許さない箇所を明示する。
層をまたぐ呼び出しのルールもここ。パッケージ図が有効。
-->

```mermaid

```

## 不変条件・境界

<!--
このシステムで常に成り立つ約束と、モジュール・システム間の境界。
重要な不変条件は「何かが存在しないこと」として表現されることが多く、コードを読むだけでは把握できない。
例:「この層から外部 API を直接呼ばない」「この状態遷移は起きない」。ここが最も価値の高い節。
-->

## 共通規約

<!-- 命名、ログ出力、例外処理、認証・認可、トランザクション境界、日時・文字コードの扱い。全機能に効くものだけ。 -->

## 非機能の実現方式

<!--
requirements.md の非機能要件を、どの仕組みで満たすか。冗長化、監視、性能、セキュリティ。
テスト方針（どのレベルで何を担保するか、使用ツール、カバレッジ基準、CI での実行）もここに書く。
テストケースは書かない。テストコードを正とする。
-->
